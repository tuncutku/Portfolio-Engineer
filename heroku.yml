build:
  docker:
    app: Dockerfile
    worker: Dockerfile
run:
  app: 
    command:
      - run_app.py
    image: app
  worker: 
    command:
      - celery worker --app=main.celery
    image: worker